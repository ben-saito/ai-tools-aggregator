---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import { getLangFromUrl, useTranslations } from '../../lib/i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout 
  title={t('about.title')}
  description={t('about.description')}
>
  <Header />
  <main class="mx-auto max-w-4xl px-4 py-12">
    <article class="prose prose-lg max-w-none">
      <h1 class="text-4xl font-bold mb-6">{t('about.pageTitle')}</h1>
      
      <p class="text-xl text-gray-600 mb-8">
        {t('about.subtitle')}
      </p>

      <section class="mb-12">
        <h2 class="text-2xl font-semibold mb-4">{t('about.mission.title')}</h2>
        <p class="text-gray-700">
          {t('about.mission.text')}
        </p>
      </section>

      <section class="mb-12">
        <h2 class="text-2xl font-semibold mb-4">{t('about.offer.title')}</h2>
        <ul class="space-y-4">
          <li class="flex items-start gap-3">
            <svg class="h-6 w-6 text-primary-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <div>
              <strong class="text-gray-900">{t('about.offer.curated.title')}</strong>
              <p class="text-gray-600 mt-1">{t('about.offer.curated.desc')}</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <svg class="h-6 w-6 text-primary-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <div>
              <strong class="text-gray-900">{t('about.offer.categories.title')}</strong>
              <p class="text-gray-600 mt-1">{t('about.offer.categories.desc')}</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <svg class="h-6 w-6 text-primary-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <div>
              <strong class="text-gray-900">{t('about.offer.updates.title')}</strong>
              <p class="text-gray-600 mt-1">{t('about.offer.updates.desc')}</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <svg class="h-6 w-6 text-primary-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <div>
              <strong class="text-gray-900">{t('about.offer.free.title')}</strong>
              <p class="text-gray-600 mt-1">{t('about.offer.free.desc')}</p>
            </div>
          </li>
        </ul>
      </section>

      <section class="mb-12">
        <h2 class="text-2xl font-semibold mb-4">{t('about.categories.title')}</h2>
        <div class="grid grid-cols-2 gap-4 sm:grid-cols-3">
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">💬</div>
            <div class="font-semibold text-sm">{t('about.categories.text')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">🎨</div>
            <div class="font-semibold text-sm">{t('about.categories.image')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">🎥</div>
            <div class="font-semibold text-sm">{t('about.categories.video')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">🎵</div>
            <div class="font-semibold text-sm">{t('about.categories.audio')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">💻</div>
            <div class="font-semibold text-sm">{t('about.categories.code')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">📈</div>
            <div class="font-semibold text-sm">{t('about.categories.marketing')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">⚡</div>
            <div class="font-semibold text-sm">{t('about.categories.productivity')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">📚</div>
            <div class="font-semibold text-sm">{t('about.categories.research')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">🖌️</div>
            <div class="font-semibold text-sm">{t('about.categories.design')}</div>
          </div>
          <div class="rounded-lg border border-gray-200 p-4">
            <div class="text-2xl mb-2">📊</div>
            <div class="font-semibold text-sm">{t('about.categories.data')}</div>
          </div>
        </div>
      </section>

      <section class="mb-12">
        <h2 class="text-2xl font-semibold mb-4">{t('about.developers.title')}</h2>
        <p class="text-gray-700 mb-4">
          {t('about.developers.text1')} 
          <a href="/ja/submit" class="text-primary-600 hover:underline">{t('about.developers.link')}</a> {t('about.developers.text2')}
        </p>
        <p class="text-gray-600 text-sm">
          {t('about.developers.premium')}
        </p>
      </section>

      <section class="rounded-lg bg-gray-50 p-8">
        <h2 class="text-2xl font-semibold mb-4">{t('about.contact.title')}</h2>
        <p class="text-gray-700 mb-4">
          {t('about.contact.text')}
        </p>
        <p class="text-gray-600">
          {t('about.contact.email')} <a href="mailto:hello@aitools.directory" class="text-primary-600 hover:underline">hello@aitools.directory</a>
        </p>
      </section>
    </article>
  </main>
</Layout>
