---
interface Props {
  title: string;
  description?: string;
  image?: string;
  canonical?: string;
}

const { 
  title, 
  description = 'Discover the best AI tools for 2026. Curated directory of 50+ AI tools across 10 categories including text generation, image creation, coding assistants, and more.',
  image = '/og-image.png',
  canonical
} = Astro.props;

const siteUrl = Astro.site || 'https://ai-tools-aggregator.vercel.app';
const fullCanonical = canonical ? `${siteUrl}${canonical}` : `${siteUrl}${Astro.url.pathname}`;
const fullImage = image.startsWith('http') ? image : `${siteUrl}${image}`;
const pageTitle = `${title} | AI Tools Hub`;
import ComparisonBar from '../components/ComparisonBar.astro';
import NewsletterSignup from '../components/NewsletterSignup.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate icon" type="image/png" href="/favicon.svg" />
    <link rel="canonical" href={fullCanonical} />
    
    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://mqksqezqbauqymftrydy.supabase.co" />
    <link rel="dns-prefetch" href="https://mqksqezqbauqymftrydy.supabase.co" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={fullCanonical} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullImage} />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={fullCanonical} />
    <meta property="twitter:title" content={pageTitle} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={fullImage} />
    
    <!-- Additional SEO -->
    <meta name="robots" content="index, follow" />
    <meta name="keywords" content="AI tools, AI directory, ChatGPT, Midjourney, AI software, artificial intelligence, machine learning tools" />
    
    <!-- Supabase CDN - dynamically loaded -->
    <script is:inline>
      (function() {
        var script = document.createElement('script');
        script.src = 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';
        script.onload = function() {
          // Create a single global Supabase client instance
          const supabaseUrl = 'https://mqksqezqbauqymftrydy.supabase.co';
          const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1xa3NxZXpxYmF1cXltZnRyeWR5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA2ODIwMDIsImV4cCI6MjA4NjI1ODAwMn0.Bf6nGo9I0skJWwFnXHHAKOBSBepYx7G8OHLw3Ih_Ytw';
          
          window.supabaseClient = window.supabase.createClient(supabaseUrl, supabaseAnonKey);
          window.__supabaseReady = true;
          window.dispatchEvent(new Event('supabase-ready'));
        };
        document.head.appendChild(script);
      })();
    </script>
    
    <!-- Google Analytics (GA4) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-WLQ3BQ6FSE" is:inline></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-WLQ3BQ6FSE', {
        page_path: window.location.pathname,
      });
    </script>
    
    <!-- Language Consistency -->
    <script is:inline>
      (function() {
        const currentPath = window.location.pathname;
        const preferredLang = localStorage.getItem('preferred-lang');
        const redirected = sessionStorage.getItem('lang-redirected');
        
        // Don't redirect if already redirected in this session or on language change
        if (redirected === 'true') return;
        
        // Detect current language from URL
        const isJaPage = currentPath.startsWith('/ja');
        const currentLang = isJaPage ? 'ja' : 'en';
        
        // Auto-redirect only if preferred language doesn't match and user hasn't manually changed it
        if (preferredLang && preferredLang !== currentLang && !redirected) {
          sessionStorage.setItem('lang-redirected', 'true');
          
          let newPath;
          if (preferredLang === 'ja' && !isJaPage) {
            newPath = '/ja' + currentPath;
          } else if (preferredLang === 'en' && isJaPage) {
            newPath = currentPath.replace(/^\/ja/, '') || '/';
          }
          
          if (newPath && newPath !== currentPath) {
            window.location.href = newPath;
          }
        }
      })();
    </script>
    
    <title>{pageTitle}</title>
  </head>
  <body class="min-h-screen bg-white text-gray-900">
    <slot />
    
    <footer class="mt-20 border-t border-gray-100 bg-gray-50 py-12">
      <div class="mx-auto max-w-4xl px-4">
        <div class="grid gap-8 md:grid-cols-2">
          <div>
            <h3 class="text-lg font-bold text-gray-900">AI Tools Aggregator</h3>
            <p class="mt-2 text-sm text-gray-600">Discover the best AI tools for 2026. Curated directory of 50+ AI tools across 10 categories.</p>
          </div>
          <div>
             <NewsletterSignup />
          </div>
        </div>
        <div class="mt-8 text-center text-sm text-gray-500">
          © {new Date().getFullYear()} AI Tools Aggregator. All rights reserved.
        </div>
      </div>
    </footer>

    <!-- Global Analytics -->
    <script is:inline>
      (async function() {
        while (!window.__supabaseReady) {
          await new Promise(resolve => setTimeout(resolve, 50));
        }

        const supabase = window.supabaseClient;

        function getSessionId() {
          let sessionId = sessionStorage.getItem('analytics_session_id');
          if (!sessionId) {
            sessionId = `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
            sessionStorage.setItem('analytics_session_id', sessionId);
          }
          return sessionId;
        }

        function extractToolId(path) {
          const match = path.match(/\/tools\/([^\/]+)/);
          return match ? match[1] : null;
        }

        async function trackEvent(event, data = {}) {
          try {
            const { data: { session: authSession } } = await supabase.auth.getSession();
            const eventData = {
              event,
              tool_id: data.tool_id || extractToolId(window.location.pathname),
              session_id: getSessionId(),
              user_id: authSession?.user?.id || null,
              referrer: document.referrer || null,
              user_agent: navigator.userAgent,
              ...data,
            };
            await supabase.from('analytics_events').insert(eventData);
          } catch (error) {
            console.debug('Analytics error:', error);
          }
        }

        trackEvent('page_view');

        const toolId = extractToolId(window.location.pathname);
        if (toolId) {
          trackEvent('tool_view', { tool_id: toolId });
        }

      })();
    </script>
    <ComparisonBar />
  </body>
</html>
