---
interface Props {
  title: string;
  description?: string;
  image?: string;
  canonical?: string;
}

const {
  title,
  description = 'Discover the best AI tools for 2026. Curated directory of 60+ AI tools across 10 categories including text generation, image creation, coding assistants, and more.',
  image = '/og-image.png',
  canonical
} = Astro.props;

// Remove trailing slash from siteUrl to avoid double slashes (siteUrl ends with /, pathname starts with /)
const siteUrl = (Astro.site?.toString() || 'https://ai-tools-aggregator.vercel.app').replace(/\/$/, '');
const fullCanonical = canonical ? `${siteUrl}${canonical}` : `${siteUrl}${Astro.url.pathname}`;
const fullImage = image.startsWith('http') ? image : `${siteUrl}${image}`;

// Optimize title tag for SEO (max 60 chars for Google search results)
let pageTitle: string;
const titleLength = title.length;

// Special case: Homepage already includes site name - don't add suffix
if (title.includes('AI Tools Hub') || title.includes('AIツールハブ')) {
  pageTitle = title;
} else if (titleLength <= 45) {
  // Short title: add full suffix (15 chars)
  pageTitle = `${title} | AI Tools Hub`;
} else if (titleLength <= 52) {
  // Medium title: add short suffix (8 chars)
  pageTitle = `${title} - AI Hub`;
} else {
  // Long title: no suffix (keep title under 60 chars)
  pageTitle = title.length <= 60 ? title : title.substring(0, 57) + '...';
}
import ComparisonBar from '../components/ComparisonBar.astro';
import NewsletterSignup from '../components/NewsletterSignup.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate icon" type="image/png" href="/favicon.svg" />
    <link rel="canonical" href={fullCanonical} />

    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

    <!-- Preload critical assets for LCP optimization -->
    <link rel="preload" href="/logo.webp" as="image" type="image/webp" />

    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://mqksqezqbauqymftrydy.supabase.co" crossorigin />
    <link rel="dns-prefetch" href="https://mqksqezqbauqymftrydy.supabase.co" />

    <!-- Dark Mode Init (FOUC prevention) -->
    <script is:inline>
      (function() {
        const theme = localStorage.getItem('theme');
        if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>

    <link rel="preconnect" href="https://www.googletagmanager.com" crossorigin />
    <link rel="dns-prefetch" href="https://www.googletagmanager.com" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={fullCanonical} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullImage} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={fullCanonical} />
    <meta property="twitter:title" content={pageTitle} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={fullImage} />

    <!-- Additional SEO -->
    <meta name="robots" content="index, follow" />
    <meta name="keywords" content="AI tools, AI directory, ChatGPT, Midjourney, AI software, artificial intelligence, machine learning tools" />

    <!-- Supabase CDN - dynamically loaded -->
    <script is:inline>
      (function() {
        var script = document.createElement('script');
        script.src = 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';
        script.onload = function() {
          // Create a single global Supabase client instance
          const supabaseUrl = 'https://mqksqezqbauqymftrydy.supabase.co';
          const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1xa3NxZXpxYmF1cXltZnRyeWR5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA2ODIwMDIsImV4cCI6MjA4NjI1ODAwMn0.Bf6nGo9I0skJWwFnXHHAKOBSBepYx7G8OHLw3Ih_Ytw';

          window.supabaseClient = window.supabase.createClient(supabaseUrl, supabaseAnonKey);
          window.__supabaseReady = true;
          window.dispatchEvent(new Event('supabase-ready'));
        };
        document.head.appendChild(script);
      })();
    </script>

    <!-- Google Analytics (GA4) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-WLQ3BQ6FSE" is:inline></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-WLQ3BQ6FSE', {
        page_path: window.location.pathname,
      });
    </script>

    <!-- Language Consistency -->
    <script is:inline>
      (function() {
        const currentPath = window.location.pathname;
        const preferredLang = localStorage.getItem('preferred-lang');
        const redirected = sessionStorage.getItem('lang-redirected');

        // Don't redirect if already redirected in this session or on language change
        if (redirected === 'true') return;

        // Detect current language from URL
        const isJaPage = currentPath.startsWith('/ja');
        const currentLang = isJaPage ? 'ja' : 'en';

        // Auto-redirect only if preferred language doesn't match and user hasn't manually changed it
        if (preferredLang && preferredLang !== currentLang && !redirected) {
          sessionStorage.setItem('lang-redirected', 'true');

          let newPath;
          if (preferredLang === 'ja' && !isJaPage) {
            newPath = '/ja' + currentPath;
          } else if (preferredLang === 'en' && isJaPage) {
            newPath = currentPath.replace(/^\/ja/, '') || '/';
          }

          if (newPath && newPath !== currentPath) {
            window.location.href = newPath;
          }
        }
      })();
    </script>

    <title>{pageTitle}</title>
  </head>
  <body class="min-h-screen bg-[var(--color-bg-primary)] text-[var(--color-text-primary)] transition-theme">
    <slot />

    <footer class="mt-20 border-t border-[var(--color-border-primary)] bg-[var(--color-bg-secondary)] py-12">
      <div class="mx-auto max-w-7xl px-4">
        <div class="grid gap-8 md:grid-cols-4">
          <!-- About Section -->
          <div>
            <h3 class="text-lg font-bold text-[var(--color-text-primary)]">AI Tools Hub</h3>
            <p class="mt-2 text-sm text-[var(--color-text-secondary)]">Discover the best AI tools for 2026. Curated directory of 60+ AI tools across 10 categories.</p>
          </div>

          <!-- Popular Categories -->
          <div>
            <h4 class="text-sm font-bold text-[var(--color-text-primary)] mb-3">Categories</h4>
            <ul class="space-y-2 text-sm">
              <li><a href="/categories/text-generation" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Text Generation</a></li>
              <li><a href="/categories/image-generation" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Image Generation</a></li>
              <li><a href="/categories/coding-assistant" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Coding Assistant</a></li>
              <li><a href="/categories/video-editing" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Video Editing</a></li>
            </ul>
          </div>

          <!-- Quick Links -->
          <div>
            <h4 class="text-sm font-bold text-[var(--color-text-primary)] mb-3">Quick Links</h4>
            <ul class="space-y-2 text-sm">
              <li><a href="/blog" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Blog</a></li>
              <li><a href="/skills" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">AI Agent Skills</a></li>
              <li><a href="/submit" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">Submit Tool</a></li>
              <li><a href="/api/v1/tools" class="text-[var(--color-text-secondary)] hover:text-primary-600 transition-colors">API</a></li>
            </ul>
          </div>

          <!-- Newsletter -->
          <div>
            <NewsletterSignup />
          </div>
        </div>

        <div class="mt-8 pt-8 border-t border-[var(--color-border-primary)] text-center text-sm text-[var(--color-text-tertiary)]">
          &copy; {new Date().getFullYear()} AI Tools Hub. All rights reserved.
        </div>
      </div>
    </footer>

    <!-- Global Analytics -->
    <script is:inline>
      (async function() {
        while (!window.__supabaseReady) {
          await new Promise(resolve => setTimeout(resolve, 50));
        }

        const supabase = window.supabaseClient;

        function getSessionId() {
          let sessionId = sessionStorage.getItem('analytics_session_id');
          if (!sessionId) {
            sessionId = `${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
            sessionStorage.setItem('analytics_session_id', sessionId);
          }
          return sessionId;
        }

        function extractToolId(path) {
          const match = path.match(/\/tools\/([^\/]+)/);
          return match ? match[1] : null;
        }

        async function trackEvent(event, data = {}) {
          try {
            const { data: { session: authSession } } = await supabase.auth.getSession();
            const eventData = {
              event,
              tool_id: data.tool_id || extractToolId(window.location.pathname),
              session_id: getSessionId(),
              user_id: authSession?.user?.id || null,
              referrer: document.referrer || null,
              user_agent: navigator.userAgent,
              ...data,
            };
            await supabase.from('analytics_events').insert(eventData);
          } catch (error) {
            console.debug('Analytics error:', error);
          }
        }

        trackEvent('page_view');

        const toolId = extractToolId(window.location.pathname);
        if (toolId) {
          trackEvent('tool_view', { tool_id: toolId });
        }

      })();
    </script>

    <!-- Scroll Reveal Observer -->
    <script is:inline>
      (function() {
        const observer = new IntersectionObserver(function(entries) {
          entries.forEach(function(entry) {
            if (entry.isIntersecting) {
              entry.target.classList.add('visible');
              observer.unobserve(entry.target);
            }
          });
        }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

        document.querySelectorAll('.reveal').forEach(function(el) {
          observer.observe(el);
        });
      })();
    </script>
    <ComparisonBar />
  </body>
</html>
