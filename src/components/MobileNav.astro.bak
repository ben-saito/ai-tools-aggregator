---
import { categories } from '../lib/tools';
---

<!-- Mobile menu button -->
<button
  id="mobile-menu-button"
  class="lg:hidden rounded-md p-2 text-gray-600 hover:bg-gray-100 hover:text-primary-600"
  aria-label="Toggle mobile menu"
>
  <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
  </svg>
</button>

<!-- Mobile menu overlay -->
<div
  id="mobile-menu"
  class="fixed inset-0 z-50 hidden bg-gray-900 bg-opacity-50"
>
  <div class="fixed inset-y-0 right-0 w-64 bg-white shadow-xl">
    <div class="flex items-center justify-between border-b border-gray-200 p-4">
      <span class="text-lg font-bold text-primary-600">Menu</span>
      <button
        id="mobile-menu-close"
        class="rounded-md p-2 text-gray-600 hover:bg-gray-100"
        aria-label="Close mobile menu"
      >
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
    <nav class="overflow-y-auto p-4">
      <a href="/" class="block py-3 text-gray-700 hover:text-primary-600">Home</a>
      <a href="/search" class="block py-3 text-gray-700 hover:text-primary-600">Search</a>
      <a href="/rankings" class="block py-3 text-gray-700 hover:text-primary-600">Rankings</a>
      
      <div class="mt-2 border-t border-gray-200 pt-2">
        <p class="mb-2 text-sm font-semibold text-gray-500">Categories</p>
        {categories.map((category) => (
          <a
            href={`/categories/${category.slug}`}
            class="block py-2 pl-3 text-sm text-gray-700 hover:text-primary-600"
          >
            <span class="mr-2">{category.icon}</span>
            {category.name}
          </a>
        ))}
      </div>
      
      <div class="mt-4 border-t border-gray-200 pt-4">
        <a href="/submit" class="block rounded-lg bg-primary-600 px-4 py-3 text-center font-semibold text-white hover:bg-primary-700">
          Submit Tool
        </a>
        <a href="/about" class="mt-2 block py-3 text-gray-700 hover:text-primary-600">About</a>
        <a href="/login" class="mt-2 block py-3 text-gray-700 hover:text-primary-600">Login</a>
      </div>
    </nav>
  </div>
</div>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const menuClose = document.getElementById('mobile-menu-close');
  const menu = document.getElementById('mobile-menu');
  
  menuButton?.addEventListener('click', () => {
    menu?.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
  });
  
  menuClose?.addEventListener('click', () => {
    menu?.classList.add('hidden');
    document.body.style.overflow = '';
  });
  
  menu?.addEventListener('click', (e) => {
    if (e.target === menu) {
      menu.classList.add('hidden');
      document.body.style.overflow = '';
    }
  });
</script>
